# Meta App credentials — found at developers.facebook.com > your app > Settings > Basic
META_APP_SECRET=your_app_secret_here

# A token you choose yourself — must match what you enter in the Meta webhook dashboard
META_VERIFY_TOKEN=your_verify_token_here

# Port to listen on (optional, defaults to 3000)
PORT=3000
